{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <h1 class="text-center mt-5">Edit User</h1>

    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('user/_delete_form.html.twig') }}
                <a class="text-white text-decoration-none" href="{{ path('user_index') }}"><button class="btn btn-primary ">Back to list</button></a>
            </div>
        </div>
    </div>
    <style>
        h1{
            color: white;
        }
        body{
            background: linear-gradient(to right bottom, #32c8dd, #00b7e7, #00a2f1, #008bf4, #126feb);
            background-repeat: no-repeat;
            padding-top: 10%;
            padding-bottom: 26%;
        }
    </style>
{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
{% endblock  %}

{% extends 'base.html.twig' %}

{% block title %}Tricount{% endblock %}

{% block body %}

    <h1 class="text-center mt-5">Tricount</h1>

    <div class="modal-dialog">
        <div class="modal-content">
            <table class="table mt-5">

                <tr class="list-group list-group-horizontal px-4 p-2">
                    <th class="list-group-item w-50">Id</th>
                    <td class="list-group-item w-50">{{ tricount.id }}</td>
                </tr>
                <tr class="list-group list-group-horizontal px-4 p-2">
                    <th class="list-group-item w-50">Title</th>
                    <td class="list-group-item w-50">{{ tricount.Title }}</td>
                </tr>
                <tr class="list-group list-group-horizontal px-4 p-2">
                    <th class="list-group-item w-50">Currency</th>
                    <td class="list-group-item w-50">{{ tricount.Currency }}</td>
                </tr>
                <tr class="list-group list-group-horizontal px-4 p-2">
                    <th class="list-group-item w-50">Description</th>
                    <td class="list-group-item w-50">{{ tricount.Description }}</td>
                </tr>
            </table>

            <div class="group_btn">
                <div class="p-3 px-5"><a class="text-white text-decoration-none" href="{{ path('tricount_index') }}"><button class="btn btn-primary ">Back to list</button></a></div>

                <div class="p-3 px-5"><a class="text-white text-decoration-none" href="{{ path('tricount_edit', {'id': tricount.id}) }}"><button class="btn btn-warning ">Edit</button></a></div>

                <div class="p-3 px-5">{{ include('tricount/_delete_form.html.twig') }}</div>
            </div>
        </div>
    </div>

    <style>
        h1{
            color: white;
        }
        body{
            background: linear-gradient(to right bottom, #32c8dd, #00b7e7, #00a2f1, #008bf4, #126feb);
            background-repeat: no-repeat;
            padding-top: 6%;
            padding-bottom: 25%;
        }
        .modal-dialog{
            margin-left: 24%;
        }
        .modal-content{
            width: 150%;
        }
        .group_btn{
            display: flex;
            padding-left: 12%;
            padding-bottom: 3%;
        }
    </style>
{% endblock %}


{% extends 'base.html.twig' %}

{% block title %}Edit Tricount{% endblock %}

{% block body %}

    <h1 class="text-center mt-5">Edit Tricount</h1>

    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                {{ include('tricount/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('tricount/_delete_form.html.twig') }}
                <a class="text-white text-decoration-none" href="{{ path('tricount_index') }}"><button class="btn btn-primary ">Back to list</button></a>
            </div>
        </div>
    </div>

    <style>
        h1{
            color: white;
        }
        body{
            background: linear-gradient(to right bottom, #32c8dd, #00b7e7, #00a2f1, #008bf4, #126feb);
            background-repeat: no-repeat;
            padding-top: 12%;
            padding-bottom: 25%;
        }
    </style>
{% endblock %}

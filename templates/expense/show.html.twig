{% extends 'base.html.twig' %}

{% block title %}Expense{% endblock %}

{% block body %}
    <h1>Expense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ expense.Title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ expense.CreatedAt ? expense.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ expense.Amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expense_index', { 'param': param }) }}">back to list</a>

    <a href="{{ path('expense_edit', {'id': expense.id}) }}">edit</a>

    {{ include('expense/_delete_form.html.twig') }}
{% endblock %}
